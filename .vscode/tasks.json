{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "gulp build",
      "type": "shell",
      "command": "gulp build",
      "options": {
        "cwd": "${workspaceFolder}/server"
      },
      "problemMatcher": []
    }
  ]
}
